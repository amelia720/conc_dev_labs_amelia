## **How to run:**

Open the main project folder in terminal.

Next run one exercise at a time using these commands:

- go run ./atomic
- go run ./barrier
- go run ./mutex
- go run ./rendezvous
- go run ./sem-ex-collatz-conjecture
- go run ./semaphore
- go run ./signalling
